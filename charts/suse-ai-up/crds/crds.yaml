apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: adapters.mcp.suse.com
spec:
  group: mcp.suse.com
  names:
    categories:
    - suse-ai
    - mcp
    kind: Adapter
    listKind: AdapterList
    plural: adapters
    shortNames:
    - mcpa
    singular: adapter
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.connectionType
      name: Connection
      type: string
    - jsonPath: .status.endpointURL
      name: Endpoint
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          Adapter is the proxy-fronted MCP server endpoint reachable at
          /api/v1/adapters/{name}/mcp. The AdapterReconciler (#17) materializes the
          backing Deployment+Service when the connection type requires one.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              AdapterSpec defines the desired state of an Adapter — a proxy-fronted MCP
              server endpoint reachable at /api/v1/adapters/{name}/mcp.
            properties:
              authentication:
                description: |-
                  Authentication describes how callers authenticate when invoking this
                  adapter through the proxy. Absent means no auth is enforced.
                properties:
                  apiKey:
                    description: APIKeyAuth describes an API key sent in a header,
                      query parameter, or cookie.
                    properties:
                      keySecretRef:
                        description: KeySecretRef is the secret + key containing the
                          API key value.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      location:
                        description: Location is where the key is placed on outbound
                          requests.
                        enum:
                        - header
                        - query
                        - cookie
                        type: string
                      name:
                        description: Name is the header / query parameter / cookie
                          name carrying the key.
                        type: string
                    required:
                    - keySecretRef
                    - location
                    - name
                    type: object
                  basic:
                    description: BasicAuth references HTTP Basic credentials in a
                      Secret.
                    properties:
                      secretRef:
                        description: SecretRef holds keys "username" and "password"
                          in the referenced Secret.
                        properties:
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                    required:
                    - secretRef
                    type: object
                  bearerToken:
                    description: |-
                      BearerTokenAuth references a static bearer token in a Secret, or marks the
                      adapter as using the proxy's dynamic token manager.
                    properties:
                      dynamic:
                        description: |-
                          Dynamic switches the proxy to its built-in token manager and ignores
                          SecretRef.
                        type: boolean
                      secretRef:
                        description: SecretRef is the secret + key holding the static
                          bearer token.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                    type: object
                  oauth:
                    description: |-
                      OAuthAuth declares the OAuth client configuration. Client secret is
                      referenced from a Secret.
                    properties:
                      authURL:
                        type: string
                      clientId:
                        type: string
                      clientSecretRef:
                        description: SecretKeySelector selects a key of a Secret.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      redirectURI:
                        type: string
                      scopes:
                        items:
                          type: string
                        type: array
                      tokenURL:
                        type: string
                    type: object
                  required:
                    description: Required indicates whether unauthenticated callers
                      are rejected.
                    type: boolean
                  type:
                    description: Type selects which sub-struct below applies.
                    enum:
                    - none
                    - bearer
                    - oauth
                    - basic
                    - apikey
                    type: string
                required:
                - required
                - type
                type: object
              connectionType:
                description: |-
                  ConnectionType selects the transport the proxy uses to reach the
                  upstream.
                enum:
                - LocalStdio
                - StreamableHttp
                - RemoteHttp
                - SSE
                - SidecarStdio
                type: string
              description:
                description: Description is a human-readable summary surfaced in UIs.
                type: string
              replicas:
                description: Replicas is the desired number of sidecar pods. Defaults
                  to 1.
                format: int32
                minimum: 0
                type: integer
              routeAssignmentRefs:
                description: |-
                  RouteAssignmentRefs name RouteAssignment CRs that grant
                  user/group access to this adapter. Empty means no ACL is enforced
                  (the cluster-wide default still applies).
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              source:
                description: Source declares where the upstream MCP server comes from.
                properties:
                  mcpServerRef:
                    description: |-
                      MCPServerRef references an MCPServer CR (same namespace) that provides
                      the sidecar template. Use this when the server entry already lives in a
                      registered MCPRegistry.
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  remoteURL:
                    description: |-
                      RemoteURL is the upstream URL for RemoteHttp / SSE connection types.
                      Ignored for stdio/sidecar variants.
                    type: string
                  sidecarConfig:
                    description: |-
                      SidecarConfig is an inline alternative to MCPServerRef for one-off
                      adapters that aren't backed by a registry entry.
                    properties:
                      args:
                        description: Args are the arguments passed to Command.
                        items:
                          type: string
                        type: array
                      command:
                        description: Command is the entrypoint binary inside the sidecar.
                        type: string
                      commandType:
                        description: CommandType identifies the launcher family (docker,
                          npx, python, pip).
                        enum:
                        - docker
                        - npx
                        - python
                        - pip
                        type: string
                      env:
                        description: |-
                          Env are environment variables exposed to the sidecar. Supports
                          SecretKeySelector / ConfigMapKeySelector via ValueFrom so secrets stay
                          out of the CR.
                        items:
                          description: EnvVar represents an environment variable present
                            in a Container.
                          properties:
                            name:
                              description: |-
                                Name of the environment variable.
                                May consist of any printable ASCII characters except '='.
                              type: string
                            value:
                              description: |-
                                Variable references $(VAR_NAME) are expanded
                                using the previously defined environment variables in the container and
                                any service environment variables. If a variable cannot be resolved,
                                the reference in the input string will be unchanged. Double $$ are reduced
                                to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                Escaped references will never be expanded, regardless of whether the variable
                                exists or not.
                                Defaults to "".
                              type: string
                            valueFrom:
                              description: Source for the environment variable's value.
                                Cannot be used if value is not empty.
                              properties:
                                configMapKeyRef:
                                  description: Selects a key of a ConfigMap.
                                  properties:
                                    key:
                                      description: The key to select.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the ConfigMap or
                                        its key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fieldRef:
                                  description: |-
                                    Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                    spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                  properties:
                                    apiVersion:
                                      description: Version of the schema the FieldPath
                                        is written in terms of, defaults to "v1".
                                      type: string
                                    fieldPath:
                                      description: Path of the field to select in
                                        the specified API version.
                                      type: string
                                  required:
                                  - fieldPath
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fileKeyRef:
                                  description: |-
                                    FileKeyRef selects a key of the env file.
                                    Requires the EnvFiles feature gate to be enabled.
                                  properties:
                                    key:
                                      description: |-
                                        The key within the env file. An invalid key will prevent the pod from starting.
                                        The keys defined within a source may consist of any printable ASCII characters except '='.
                                        During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                      type: string
                                    optional:
                                      default: false
                                      description: |-
                                        Specify whether the file or its key must be defined. If the file or key
                                        does not exist, then the env var is not published.
                                        If optional is set to true and the specified key does not exist,
                                        the environment variable will not be set in the Pod's containers.

                                        If optional is set to false and the specified key does not exist,
                                        an error will be returned during Pod creation.
                                      type: boolean
                                    path:
                                      description: |-
                                        The path within the volume from which to select the file.
                                        Must be relative and may not contain the '..' path or start with '..'.
                                      type: string
                                    volumeName:
                                      description: The name of the volume mount containing
                                        the env file.
                                      type: string
                                  required:
                                  - key
                                  - path
                                  - volumeName
                                  type: object
                                  x-kubernetes-map-type: atomic
                                resourceFieldRef:
                                  description: |-
                                    Selects a resource of the container: only resources limits and requests
                                    (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                  properties:
                                    containerName:
                                      description: 'Container name: required for volumes,
                                        optional for env vars'
                                      type: string
                                    divisor:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      description: Specifies the output format of
                                        the exposed resources, defaults to "1"
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    resource:
                                      description: 'Required: resource to select'
                                      type: string
                                  required:
                                  - resource
                                  type: object
                                  x-kubernetes-map-type: atomic
                                secretKeyRef:
                                  description: Selects a key of a secret in the pod's
                                    namespace
                                  properties:
                                    key:
                                      description: The key of the secret to select
                                        from.  Must be a valid secret key.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the Secret or its
                                        key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                              type: object
                          required:
                          - name
                          type: object
                        type: array
                      image:
                        description: Image is the container image when CommandType=docker.
                        type: string
                      port:
                        description: |-
                          Port the sidecar listens on; defaults to a controller-assigned port if
                          unset.
                        format: int32
                        maximum: 65535
                        minimum: 1
                        type: integer
                      projectURL:
                        description: ProjectURL points at the upstream MCP server
                          source.
                        type: string
                      releaseURL:
                        description: ReleaseURL points at the specific release used.
                        type: string
                      source:
                        description: Source records the origin of the config (e.g.
                          registry name, manual).
                        type: string
                    required:
                    - command
                    - commandType
                    type: object
                type: object
              variables:
                additionalProperties:
                  type: string
                description: |-
                  Variables are name/value pairs substituted into the sidecar config at
                  reconcile time (the `{{var.name}}` template form used by the existing
                  adapter service).
                type: object
            required:
            - connectionType
            - source
            type: object
          status:
            description: AdapterStatus reflects the observed state of an Adapter.
            properties:
              conditions:
                description: Conditions track Ready / Synced / Healthy semantics.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              endpointURL:
                description: |-
                  EndpointURL is the proxy-fronted MCP URL clients use to reach this
                  adapter.
                type: string
              lastCapabilitySnapshotTime:
                description: |-
                  LastCapabilitySnapshotTime is when the proxy last cached the upstream
                  MCP capabilities for this adapter.
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the spec generation the controller
                  last acted on.
                format: int64
                type: integer
              phase:
                description: Phase is the high-level rollup of Conditions.
                enum:
                - Pending
                - Provisioning
                - Ready
                - Degraded
                - Failed
                - Terminating
                type: string
              sidecarDeploymentRef:
                description: |-
                  SidecarDeploymentRef points at the Deployment AdapterReconciler owns
                  for this adapter, when one exists (stdio/sidecar transports).
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: agents.mcp.suse.com
spec:
  group: mcp.suse.com
  names:
    categories:
    - suse-ai
    - mcp
    kind: Agent
    listKind: AgentList
    plural: agents
    shortNames:
    - agent
    singular: agent
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.protocol
      name: Protocol
      type: string
    - jsonPath: .status.mode
      name: Mode
      type: string
    - jsonPath: .status.endpointURL
      name: Endpoint
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          Agent declares an agent served on top of MCP, supporting multiple agent
          protocols (A2A and future). The AgentReconciler (#17) registers the
          agent with the proxy in-process or, when Spec.Runtime is set, owns an
          external runtime Deployment+Service.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              AgentSpec declares an agent served on top of MCP. The protocol selects
              which registered AgentProtocol implementation (in pkg/services/agents/)
              translates between the agent surface and the underlying tool calls.
            properties:
              acl:
                description: ACL names RouteAssignment CRs governing who can invoke
                  this agent.
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              description:
                description: Description is a human-readable summary shown in UIs.
                type: string
              protocol:
                description: |-
                  Protocol identifies the AgentProtocol implementation that serves this
                  agent (e.g. "a2a", "smartagents"). Free-form string so new protocols
                  can be added without CRD churn; the proxy fails the agent
                  (Status.Phase=Failed, Condition ProtocolUnknown=True) if no
                  implementation is registered.
                minLength: 1
                type: string
              runtime:
                description: |-
                  Runtime, when set, causes the controller to deploy an external
                  agent-runtime Deployment+Service. When nil the agent is served
                  in-process by the proxy translating the protocol to MCP calls.
                properties:
                  args:
                    description: Args overrides the image's default command arguments.
                    items:
                      type: string
                    type: array
                  env:
                    description: |-
                      Env are environment variables passed to the runtime. Supports
                      SecretKeySelector / ConfigMapKeySelector via ValueFrom.
                    items:
                      description: EnvVar represents an environment variable present
                        in a Container.
                      properties:
                        name:
                          description: |-
                            Name of the environment variable.
                            May consist of any printable ASCII characters except '='.
                          type: string
                        value:
                          description: |-
                            Variable references $(VAR_NAME) are expanded
                            using the previously defined environment variables in the container and
                            any service environment variables. If a variable cannot be resolved,
                            the reference in the input string will be unchanged. Double $$ are reduced
                            to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                            "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                            Escaped references will never be expanded, regardless of whether the variable
                            exists or not.
                            Defaults to "".
                          type: string
                        valueFrom:
                          description: Source for the environment variable's value.
                            Cannot be used if value is not empty.
                          properties:
                            configMapKeyRef:
                              description: Selects a key of a ConfigMap.
                              properties:
                                key:
                                  description: The key to select.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the ConfigMap or its
                                    key must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                            fieldRef:
                              description: |-
                                Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                              properties:
                                apiVersion:
                                  description: Version of the schema the FieldPath
                                    is written in terms of, defaults to "v1".
                                  type: string
                                fieldPath:
                                  description: Path of the field to select in the
                                    specified API version.
                                  type: string
                              required:
                              - fieldPath
                              type: object
                              x-kubernetes-map-type: atomic
                            fileKeyRef:
                              description: |-
                                FileKeyRef selects a key of the env file.
                                Requires the EnvFiles feature gate to be enabled.
                              properties:
                                key:
                                  description: |-
                                    The key within the env file. An invalid key will prevent the pod from starting.
                                    The keys defined within a source may consist of any printable ASCII characters except '='.
                                    During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                  type: string
                                optional:
                                  default: false
                                  description: |-
                                    Specify whether the file or its key must be defined. If the file or key
                                    does not exist, then the env var is not published.
                                    If optional is set to true and the specified key does not exist,
                                    the environment variable will not be set in the Pod's containers.

                                    If optional is set to false and the specified key does not exist,
                                    an error will be returned during Pod creation.
                                  type: boolean
                                path:
                                  description: |-
                                    The path within the volume from which to select the file.
                                    Must be relative and may not contain the '..' path or start with '..'.
                                  type: string
                                volumeName:
                                  description: The name of the volume mount containing
                                    the env file.
                                  type: string
                              required:
                              - key
                              - path
                              - volumeName
                              type: object
                              x-kubernetes-map-type: atomic
                            resourceFieldRef:
                              description: |-
                                Selects a resource of the container: only resources limits and requests
                                (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                              properties:
                                containerName:
                                  description: 'Container name: required for volumes,
                                    optional for env vars'
                                  type: string
                                divisor:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  description: Specifies the output format of the
                                    exposed resources, defaults to "1"
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                resource:
                                  description: 'Required: resource to select'
                                  type: string
                              required:
                              - resource
                              type: object
                              x-kubernetes-map-type: atomic
                            secretKeyRef:
                              description: Selects a key of a secret in the pod's
                                namespace
                              properties:
                                key:
                                  description: The key of the secret to select from.  Must
                                    be a valid secret key.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the Secret or its key
                                    must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  image:
                    description: Image is the container image for the agent runtime.
                    type: string
                  port:
                    description: Port the runtime listens on.
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                  replicas:
                    description: Replicas is the desired runtime replica count. Defaults
                      to 1.
                    format: int32
                    minimum: 0
                    type: integer
                  resources:
                    description: Resources are the container resource requests/limits.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                required:
                - image
                type: object
              tools:
                description: |-
                  Tools lists the Adapters / VirtualMCPRoutes the agent is allowed to
                  call. Acts as a hard ACL enforced by the proxy at request time. Empty
                  means the agent has no tool access (it can still respond to protocol
                  calls that don't require a tool).
                items:
                  description: |-
                    AgentToolRef is one tool reference the agent is allowed to invoke. Exactly
                    one of AdapterRef / VirtualMCPRouteRef must be set. The proxy enforces
                    the ACL at request time, so anything the agent calls outside this list is
                    rejected even if the underlying adapter is otherwise reachable.
                  properties:
                    adapterRef:
                      description: AdapterRef references an Adapter CR (same namespace).
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    virtualMCPRouteRef:
                      description: VirtualMCPRouteRef references a VirtualMCPRoute
                        CR (same namespace).
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                  type: object
                type: array
            required:
            - protocol
            type: object
          status:
            description: AgentStatus reflects the observed state of an Agent.
            properties:
              conditions:
                description: Conditions track Ready / ProtocolUnknown / ToolMissing
                  semantics.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              endpointURL:
                description: |-
                  EndpointURL is where clients reach the agent (e.g.
                  http://proxy/api/v1/agents/<name>/...).
                type: string
              mode:
                description: |-
                  Mode tells callers whether the agent runs in-process inside the proxy
                  or as a separate Deployment.
                enum:
                - InProcess
                - External
                type: string
              observedGeneration:
                description: ObservedGeneration is the spec generation the controller
                  last acted on.
                format: int64
                type: integer
              phase:
                description: Phase is the high-level rollup of Conditions.
                enum:
                - Pending
                - Registered
                - Provisioning
                - Ready
                - Degraded
                - Failed
                type: string
              runtimeDeploymentRef:
                description: |-
                  RuntimeDeploymentRef points at the Deployment AgentReconciler owns
                  when Mode=External.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: groups.mcp.suse.com
spec:
  group: mcp.suse.com
  names:
    categories:
    - suse-ai
    - mcp
    kind: Group
    listKind: GroupList
    plural: groups
    shortNames:
    - mcpgroup
    singular: group
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.memberCount
      name: Members
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          Group is a named bundle of Users plus a permission set, referenced by
          RouteAssignment to grant access to Adapters / VirtualMCPRoutes / Agents.
          Distinct from Kubernetes RBAC groups, which govern operator access.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              GroupSpec declares a proxy-internal group: a named bundle of Users plus a
              permission set. Groups are the primary unit RouteAssignment refers to;
              granting a group access to a route implicitly grants every member.
            properties:
              description:
                description: Description is a free-form summary surfaced in UIs.
                type: string
              displayName:
                description: DisplayName is the human-readable name shown in UIs.
                type: string
              members:
                description: |-
                  Members names User CRs (same namespace) that belong to this group.
                  Denormalized with User.Spec.Groups; the GroupReconciler keeps both
                  edges in sync (matches the existing model where membership is stored
                  on both sides).
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              permissions:
                description: |-
                  Permissions are coarse-grained permission strings (e.g.
                  "server:read", "adapter:create") evaluated by the proxy at request
                  time. Free-form to avoid CRD churn as new permission verbs are added.
                items:
                  type: string
                type: array
            type: object
          status:
            description: GroupStatus reflects the observed state of a Group.
            properties:
              conditions:
                description: Conditions track Ready / MembersResolved semantics.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              memberCount:
                description: MemberCount is the number of resolved User CRs in Spec.Members.
                format: int32
                type: integer
              observedGeneration:
                description: ObservedGeneration is the spec generation the controller
                  last acted on.
                format: int64
                type: integer
              phase:
                description: Phase is the high-level rollup of Conditions.
                enum:
                - Pending
                - Ready
                - Failed
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: mcpregistries.mcp.suse.com
spec:
  group: mcp.suse.com
  names:
    categories:
    - suse-ai
    - mcp
    kind: MCPRegistry
    listKind: MCPRegistryList
    plural: mcpregistries
    shortNames:
    - mcpreg
    singular: mcpregistry
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.priority
      name: Priority
      type: integer
    - jsonPath: .status.observedServerCount
      name: Servers
      type: integer
    - jsonPath: .status.lastSyncTime
      name: LastSync
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          MCPRegistry is one source of MCPServer entries. Multiple registries
          coexist; Spec.Priority resolves name conflicts across them. The
          MCPRegistryReconciler (#17) creates child MCPServer CRs with
          OwnerReference set to this registry so deletion cascades automatically.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              MCPRegistrySpec defines the desired state of an MCPRegistry — one source
              of MCPServer entries. Multiple registries can coexist; Priority resolves
              name conflicts across them.
            properties:
              priority:
                description: |-
                  Priority resolves name conflicts when two registries provide an
                  MCPServer with the same name. Higher priority wins; ties are broken
                  by creation timestamp (older wins). Negative values are allowed.
                format: int32
                type: integer
              refreshInterval:
                description: |-
                  RefreshInterval controls how often the controller re-syncs from
                  Source. Defaults to 5m when unset. Ignored for inline sources (no
                  external state to poll).
                type: string
              source:
                description: Source is where this registry's entries come from.
                properties:
                  configMapRef:
                    description: |-
                      ConfigMapRef references a ConfigMap (same namespace) containing the
                      registry YAML in a single key. Useful for GitOps flows.
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  inline:
                    description: |-
                      Inline is an embedded list of MCPServer entries. Useful for small or
                      project-local registries with no external source.
                    items:
                      description: |-
                        MCPServerSpec is the declarative form of an MCP server entry. The same
                        shape is used both as a standalone CR's Spec and as an entry inside
                        MCPRegistrySpec.Source.Inline.
                      properties:
                        categories:
                          description: Categories group servers by domain (e.g. "monitoring",
                            "ci").
                          items:
                            type: string
                          type: array
                        description:
                          description: Description is a short summary of what this
                            server provides.
                          type: string
                        displayName:
                          description: DisplayName is the human-readable name shown
                            in UIs.
                          type: string
                        image:
                          description: Image is the primary container image for containerized
                            servers.
                          type: string
                        packages:
                          description: Packages are the shipped artifacts (OCI image,
                            npm package, etc.).
                          items:
                            description: |-
                              MCPServerPackage describes one shipped artifact (OCI image, npm package,
                              etc.) that runs the MCP server.
                            properties:
                              environmentVariables:
                                description: EnvironmentVariables documents the env
                                  vars the package recognizes.
                                items:
                                  description: |-
                                    MCPServerEnvVar describes a runtime environment variable the server
                                    expects. It is purely declarative — the Adapter that consumes this
                                    MCPServer is responsible for satisfying secret values.
                                  properties:
                                    default:
                                      description: Default is the value used when
                                        no Adapter override is supplied.
                                      type: string
                                    description:
                                      description: Description is a short human-readable
                                        note about the variable.
                                      type: string
                                    format:
                                      description: |-
                                        Format is a hint about the expected value shape (string, number,
                                        boolean).
                                      enum:
                                      - string
                                      - number
                                      - boolean
                                      type: string
                                    isSecret:
                                      description: |-
                                        IsSecret marks the variable as sensitive so UIs render it accordingly
                                        and Adapter authors know to supply it via Secret rather than inline.
                                      type: boolean
                                    name:
                                      description: Name is the environment variable
                                        name.
                                      type: string
                                  required:
                                  - name
                                  type: object
                                type: array
                              identifier:
                                description: |-
                                  Identifier is the package identifier inside RegistryType (e.g.
                                  `docker.io/user/image:tag`).
                                type: string
                              registryType:
                                description: RegistryType identifies the package registry
                                  (oci, npm, pypi, etc.).
                                type: string
                              transport:
                                description: Transport is how the proxy speaks to
                                  this package once running.
                                properties:
                                  type:
                                    description: Type is the transport identifier
                                      (stdio, sse, websocket, http).
                                    enum:
                                    - stdio
                                    - sse
                                    - websocket
                                    - http
                                    type: string
                                required:
                                - type
                                type: object
                            required:
                            - identifier
                            - registryType
                            - transport
                            type: object
                          type: array
                        port:
                          description: Port is the network port the server listens
                            on.
                          format: int32
                          maximum: 65535
                          minimum: 1
                          type: integer
                        repository:
                          description: Repository describes where the server source
                            lives.
                          properties:
                            source:
                              description: Source identifies the hosting service (github,
                                gitlab, etc.).
                              type: string
                            url:
                              description: URL is the repository URL.
                              type: string
                          required:
                          - url
                          type: object
                        tags:
                          description: Tags are free-form labels used for search/filter.
                          items:
                            type: string
                          type: array
                        url:
                          description: URL is the endpoint for remote-hosted MCP servers.
                          type: string
                        version:
                          description: Version is the upstream version this entry
                            describes.
                          type: string
                      type: object
                    type: array
                  url:
                    description: URL is a remote registry YAML endpoint (HTTP or HTTPS).
                    type: string
                type: object
            required:
            - source
            type: object
          status:
            description: MCPRegistryStatus reflects the observed state of an MCPRegistry.
            properties:
              conditions:
                description: Conditions track Ready / Synced semantics.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastSyncTime:
                description: |-
                  LastSyncTime is when the controller last successfully reconciled
                  Source into child MCPServer entries.
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the spec generation the controller
                  last acted on.
                format: int64
                type: integer
              observedServerCount:
                description: |-
                  ObservedServerCount is the number of MCPServer entries currently
                  owned by this registry.
                format: int32
                type: integer
              phase:
                description: Phase is the high-level rollup of Conditions.
                enum:
                - Pending
                - Syncing
                - Ready
                - Failed
                type: string
              syncError:
                description: |-
                  SyncError carries the most recent fetch/parse error, if any. Cleared
                  once a sync succeeds.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: mcpservers.mcp.suse.com
spec:
  group: mcp.suse.com
  names:
    categories:
    - suse-ai
    - mcp
    kind: MCPServer
    listKind: MCPServerList
    plural: mcpservers
    shortNames:
    - mcpsrv
    singular: mcpserver
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.sourceRegistry
      name: Registry
      type: string
    - jsonPath: .status.priority
      name: Priority
      type: integer
    - jsonPath: .spec.image
      name: Image
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          MCPServer is one entry per MCP server definition. Typically created by an
          MCPRegistry controller (via OwnerReference) from a source registry; can
          also be created standalone for one-off declarative entries.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              MCPServerSpec is the declarative form of an MCP server entry. The same
              shape is used both as a standalone CR's Spec and as an entry inside
              MCPRegistrySpec.Source.Inline.
            properties:
              categories:
                description: Categories group servers by domain (e.g. "monitoring",
                  "ci").
                items:
                  type: string
                type: array
              description:
                description: Description is a short summary of what this server provides.
                type: string
              displayName:
                description: DisplayName is the human-readable name shown in UIs.
                type: string
              image:
                description: Image is the primary container image for containerized
                  servers.
                type: string
              packages:
                description: Packages are the shipped artifacts (OCI image, npm package,
                  etc.).
                items:
                  description: |-
                    MCPServerPackage describes one shipped artifact (OCI image, npm package,
                    etc.) that runs the MCP server.
                  properties:
                    environmentVariables:
                      description: EnvironmentVariables documents the env vars the
                        package recognizes.
                      items:
                        description: |-
                          MCPServerEnvVar describes a runtime environment variable the server
                          expects. It is purely declarative — the Adapter that consumes this
                          MCPServer is responsible for satisfying secret values.
                        properties:
                          default:
                            description: Default is the value used when no Adapter
                              override is supplied.
                            type: string
                          description:
                            description: Description is a short human-readable note
                              about the variable.
                            type: string
                          format:
                            description: |-
                              Format is a hint about the expected value shape (string, number,
                              boolean).
                            enum:
                            - string
                            - number
                            - boolean
                            type: string
                          isSecret:
                            description: |-
                              IsSecret marks the variable as sensitive so UIs render it accordingly
                              and Adapter authors know to supply it via Secret rather than inline.
                            type: boolean
                          name:
                            description: Name is the environment variable name.
                            type: string
                        required:
                        - name
                        type: object
                      type: array
                    identifier:
                      description: |-
                        Identifier is the package identifier inside RegistryType (e.g.
                        `docker.io/user/image:tag`).
                      type: string
                    registryType:
                      description: RegistryType identifies the package registry (oci,
                        npm, pypi, etc.).
                      type: string
                    transport:
                      description: Transport is how the proxy speaks to this package
                        once running.
                      properties:
                        type:
                          description: Type is the transport identifier (stdio, sse,
                            websocket, http).
                          enum:
                          - stdio
                          - sse
                          - websocket
                          - http
                          type: string
                      required:
                      - type
                      type: object
                  required:
                  - identifier
                  - registryType
                  - transport
                  type: object
                type: array
              port:
                description: Port is the network port the server listens on.
                format: int32
                maximum: 65535
                minimum: 1
                type: integer
              repository:
                description: Repository describes where the server source lives.
                properties:
                  source:
                    description: Source identifies the hosting service (github, gitlab,
                      etc.).
                    type: string
                  url:
                    description: URL is the repository URL.
                    type: string
                required:
                - url
                type: object
              tags:
                description: Tags are free-form labels used for search/filter.
                items:
                  type: string
                type: array
              url:
                description: URL is the endpoint for remote-hosted MCP servers.
                type: string
              version:
                description: Version is the upstream version this entry describes.
                type: string
            type: object
          status:
            description: MCPServerStatus reflects the observed state of an MCPServer.
            properties:
              conditions:
                description: Conditions track Ready / Conflict semantics.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the spec generation the controller
                  last acted on.
                format: int64
                type: integer
              phase:
                description: Phase is the high-level rollup of Conditions.
                enum:
                - Pending
                - Active
                - Conflict
                - Inactive
                type: string
              priority:
                description: |-
                  Priority is copied from the originating MCPRegistry.Spec.Priority and
                  used by other registries' reconcilers to detect/resolve name conflicts.
                format: int32
                type: integer
              sourceRegistry:
                description: |-
                  SourceRegistry is the name of the MCPRegistry that owns this entry.
                  Set automatically by MCPRegistryReconciler via OwnerReference; surfaced
                  here for convenient querying.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: plugins.mcp.suse.com
spec:
  group: mcp.suse.com
  names:
    categories:
    - suse-ai
    - mcp
    kind: Plugin
    listKind: PluginList
    plural: plugins
    shortNames:
    - mcpplug
    singular: plugin
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.serviceType
      name: Type
      type: string
    - jsonPath: .spec.serviceURL
      name: URL
      type: string
    - jsonPath: .status.healthy
      name: Healthy
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          Plugin is the declarative form of POST /plugins/register: a third-party
          HTTP service the proxy fronts to extend its capabilities (smartagents
          runtimes, registry providers, virtualmcp gateways). The PluginReconciler
          (#19+) projects Plugin CRs into the in-process PluginServiceManager and
          keeps Status in sync with health-probe results.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              PluginSpec is the declarative form of POST /plugins/register: a
              third-party HTTP service that extends the proxy with capabilities the
              proxy itself does not implement (smartagents runtimes, external registry
              providers, virtualmcp gateways). The PluginReconciler (#19+) projects
              this CR into the in-process PluginServiceManager so requests matching
              Capabilities[].Path are forwarded to ServiceURL.
            properties:
              capabilities:
                description: |-
                  Capabilities lists the API surfaces this plugin handles. Empty
                  disables routing (the plugin remains visible to operators but
                  receives no traffic).
                items:
                  description: |-
                    PluginCapability describes one API surface the plugin exposes through the
                    proxy. The proxy uses Path/Methods to route incoming requests to the
                    plugin; Description is informational only.
                  properties:
                    description:
                      description: Description is a free-form summary surfaced in
                        UIs.
                      type: string
                    methods:
                      description: |-
                        Methods are the HTTP methods the plugin accepts on Path. Empty means
                        all methods are forwarded.
                      items:
                        type: string
                      type: array
                    path:
                      description: |-
                        Path is the API path the plugin handles, with optional wildcards
                        (e.g. "/v1/*", "/agents/*").
                      minLength: 1
                      type: string
                  required:
                  - path
                  type: object
                type: array
              description:
                description: Description is a free-form summary surfaced in UIs.
                type: string
              healthCheck:
                description: |-
                  HealthCheck configures the controller's liveness probe against the
                  plugin. When nil the controller uses defaults (GET /health every
                  30s, 5s timeout).
                properties:
                  intervalSeconds:
                    description: IntervalSeconds is the polling cadence. Defaults
                      to 30 when unset.
                    format: int32
                    minimum: 1
                    type: integer
                  path:
                    description: |-
                      Path is the HTTP path to poll on the plugin's ServiceURL. Defaults
                      to "/health" when unset.
                    type: string
                  timeoutSeconds:
                    description: TimeoutSeconds caps each probe. Defaults to 5 when
                      unset.
                    format: int32
                    minimum: 1
                    type: integer
                type: object
              serviceType:
                description: ServiceType selects which manager wires up this plugin.
                enum:
                - smartagents
                - registry
                - virtualmcp
                type: string
              serviceURL:
                description: |-
                  ServiceURL is the base URL the proxy forwards matched requests to.
                  Must be reachable from the proxy pod (cluster-internal Service DNS
                  or a routable external URL).
                minLength: 1
                type: string
              version:
                description: |-
                  Version is a free-form version string surfaced in UIs and matched
                  against capability filters. Plugin authors should follow semver.
                type: string
            required:
            - serviceType
            - serviceURL
            type: object
          status:
            description: PluginStatus reflects the observed state of a Plugin.
            properties:
              conditions:
                description: Conditions track Ready / Registered / Healthy semantics.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              healthy:
                description: |-
                  Healthy is the last health-probe result. False means the most
                  recent probe failed or timed out.
                type: boolean
              lastHealthCheckMessage:
                description: |-
                  LastHealthCheckMessage carries the most recent probe error string
                  when Healthy=false. Empty on success.
                type: string
              lastHealthCheckTime:
                description: |-
                  LastHealthCheckTime is when the controller last completed a health
                  probe against the plugin.
                format: date-time
                type: string
              observedCapabilities:
                description: |-
                  ObservedCapabilities is the capability list the controller has
                  successfully wired into the in-process PluginServiceManager. May
                  diverge from Spec.Capabilities during a rollout.
                items:
                  description: |-
                    PluginCapability describes one API surface the plugin exposes through the
                    proxy. The proxy uses Path/Methods to route incoming requests to the
                    plugin; Description is informational only.
                  properties:
                    description:
                      description: Description is a free-form summary surfaced in
                        UIs.
                      type: string
                    methods:
                      description: |-
                        Methods are the HTTP methods the plugin accepts on Path. Empty means
                        all methods are forwarded.
                      items:
                        type: string
                      type: array
                    path:
                      description: |-
                        Path is the API path the plugin handles, with optional wildcards
                        (e.g. "/v1/*", "/agents/*").
                      minLength: 1
                      type: string
                  required:
                  - path
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration is the spec generation the controller
                  last acted on.
                format: int64
                type: integer
              phase:
                description: Phase is the high-level rollup of Conditions.
                enum:
                - Pending
                - Registered
                - Healthy
                - Unhealthy
                - Failed
                type: string
              registeredAt:
                description: |-
                  RegisteredAt is when the plugin was first projected into the
                  in-process manager (replaces the HTTP registration timestamp).
                format: date-time
                type: string
              responseTimeMillis:
                description: |-
                  ResponseTimeMillis is the most recent probe latency in
                  milliseconds, surfaced for quick triage.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: routeassignments.mcp.suse.com
spec:
  group: mcp.suse.com
  names:
    categories:
    - suse-ai
    - mcp
    kind: RouteAssignment
    listKind: RouteAssignmentList
    plural: routeassignments
    shortNames:
    - racl
    singular: routeassignment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.permissions
      name: Permissions
      type: string
    - jsonPath: .status.resolvedSubjectCount
      name: Subjects
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          RouteAssignment grants Users/Groups access to the Adapters,
          VirtualMCPRoutes, and Agents that reference this assignment via
          Spec.RouteAssignmentRefs / Spec.ACL. The proxy evaluates assignments at
          request time and rejects calls from unmatched subjects.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              RouteAssignmentSpec declares who may invoke the routes (Adapters,
              VirtualMCPRoutes, Agents) that reference this assignment. The
              target-references-assignment direction generalizes the old
              per-server-id model: a single RouteAssignment can be reused across many
              adapters/routes/agents instead of being scoped to one server.
            properties:
              autoSpawn:
                description: |-
                  AutoSpawn instructs the proxy to spawn the backing adapter/sidecar
                  on first matched request, rather than requiring it to be pre-warmed.
                type: boolean
              description:
                description: Description is a free-form summary surfaced in UIs.
                type: string
              groups:
                description: |-
                  Groups names Group CRs (same namespace) granted access by this
                  assignment. A user matches if they appear in Users directly or via
                  any Group in this list.
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              permissions:
                description: |-
                  Permissions selects the access level granted to matched users.
                  Defaults to "read" when unset.
                enum:
                - read
                - write
                - admin
                type: string
              users:
                description: |-
                  Users names User CRs (same namespace) granted access by this
                  assignment.
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
            type: object
          status:
            description: RouteAssignmentStatus reflects the observed state of a RouteAssignment.
            properties:
              conditions:
                description: |-
                  Conditions track Ready / SubjectsResolved / ReferencedByRoute
                  semantics.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the spec generation the controller
                  last acted on.
                format: int64
                type: integer
              phase:
                description: Phase is the high-level rollup of Conditions.
                enum:
                - Pending
                - Ready
                - Failed
                type: string
              resolvedSubjectCount:
                description: |-
                  ResolvedSubjectCount is the total number of distinct users granted by
                  this assignment after expanding Groups.
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: users.mcp.suse.com
spec:
  group: mcp.suse.com
  names:
    categories:
    - suse-ai
    - mcp
    kind: User
    listKind: UserList
    plural: users
    shortNames:
    - mcpuser
    singular: user
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.authProvider
      name: Provider
      type: string
    - jsonPath: .spec.email
      name: Email
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          User is a proxy-internal identity used by RouteAssignment to govern access
          to Adapters / VirtualMCPRoutes / Agents through the proxy data plane.
          Distinct from Kubernetes RBAC subjects, which govern operator access.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              UserSpec declares a proxy-internal user. This is the proxy's auth model,
              complementary to native Kubernetes RBAC: cluster admins manage operator
              access via RBAC, while User/Group/RouteAssignment govern who can invoke
              MCP tools through the proxy data plane.
            properties:
              authProvider:
                description: |-
                  AuthProvider selects which identity source backs this user. Defaults
                  to "local" if unset.
                enum:
                - local
                - github
                - rancher
                type: string
              displayName:
                description: DisplayName is the human-readable name shown in UIs.
                type: string
              email:
                description: |-
                  Email is the user's email address; used as the login identifier for
                  local-provider users and for display when federating.
                type: string
              externalID:
                description: |-
                  ExternalID is the provider-side identifier (e.g. GitHub user id) used
                  to correlate federated logins back to this User. Ignored when
                  AuthProvider=local.
                type: string
              groups:
                description: |-
                  Groups names Group CRs (same namespace) this user belongs to. The
                  reverse edge lives in Group.Spec.Members; both are kept in sync by the
                  UserReconciler (denormalized to match the existing model). Membership
                  drives RouteAssignment matching at request time.
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              passwordSecretRef:
                description: |-
                  PasswordSecretRef references a Secret holding the bcrypt password hash
                  for local-provider users. The key inside the Secret holds the hash;
                  the plaintext password is never stored in the CR. Ignored when
                  AuthProvider is anything other than "local".
                properties:
                  key:
                    description: The key of the secret to select from.  Must be a
                      valid secret key.
                    type: string
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                  optional:
                    description: Specify whether the Secret or its key must be defined
                    type: boolean
                required:
                - key
                type: object
                x-kubernetes-map-type: atomic
              providerGroups:
                description: |-
                  ProviderGroups are the raw group/team strings asserted by the external
                  provider (e.g. "myorg/sre"). The proxy maps these to Group CRs at
                  login time; surfaced here for auditing and debugging.
                items:
                  type: string
                type: array
            type: object
          status:
            description: UserStatus reflects the observed state of a User.
            properties:
              conditions:
                description: Conditions track Ready / GroupsResolved semantics.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastLoginTime:
                description: LastLoginTime is when the user last successfully authenticated.
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the spec generation the controller
                  last acted on.
                format: int64
                type: integer
              passwordChangedTime:
                description: |-
                  PasswordChangedTime is when the local-provider password was last
                  rotated. Nil for non-local providers.
                format: date-time
                type: string
              phase:
                description: Phase is the high-level rollup of Conditions.
                enum:
                - Pending
                - Active
                - Disabled
                - Failed
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: virtualmcproutes.mcp.suse.com
spec:
  group: mcp.suse.com
  names:
    categories:
    - suse-ai
    - mcp
    kind: VirtualMCPRoute
    listKind: VirtualMCPRouteList
    plural: virtualmcproutes
    shortNames:
    - vroute
    singular: virtualmcproute
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.exposedAs
      name: ExposedAs
      type: string
    - jsonPath: .status.entryCount
      name: Entries
      type: integer
    - jsonPath: .status.lastResolvedTime
      name: LastResolved
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          VirtualMCPRoute composes a new MCP endpoint out of selected tools,
          resources, and prompts from one or more Adapters / MCPServers. The
          VirtualMCPRouteReconciler (#17) resolves the flattened catalog and
          publishes it into the proxy's in-process routing table — no Kubernetes
          workload is created for a route.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              VirtualMCPRouteSpec defines the desired state of a VirtualMCPRoute — a
              composed MCP endpoint built from selected tools/resources of one or more
              Adapters / MCPServers. Served at /api/v1/vroutes/{exposedAs}/mcp by the
              proxy.
            properties:
              acl:
                description: |-
                  ACL names RouteAssignment CRs governing who can invoke this route.
                  Empty means no per-route ACL is enforced (the cluster-wide default
                  still applies).
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              description:
                description: Description is a human-readable summary shown in UIs.
                type: string
              exposedAs:
                description: |-
                  ExposedAs is the path segment under /api/v1/vroutes/ where this route
                  is served. Defaults to metadata.name when unset.
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
              sources:
                description: |-
                  Sources are the Adapter / MCPServer references contributing entries
                  to this route.
                items:
                  description: |-
                    VirtualMCPSource is one Adapter or MCPServer contributing entries to the
                    virtual route. Exactly one of AdapterRef / MCPServerRef must be set.
                  properties:
                    adapterRef:
                      description: |-
                        AdapterRef references an Adapter CR (same namespace) to draw entries
                        from.
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    mcpServerRef:
                      description: |-
                        MCPServerRef references an MCPServer CR (same namespace) to draw
                        entries from.
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    prompts:
                      description: Prompts selects which prompts to include.
                      properties:
                        all:
                          description: All matches every entry in the source's catalog.
                          type: boolean
                        names:
                          description: Names is an explicit allow-list of entry names.
                          items:
                            type: string
                          type: array
                        prefix:
                          description: Prefix matches any entry whose name starts
                            with this string.
                          type: string
                        regex:
                          description: Regex matches any entry whose name matches
                            this Go regexp.
                          type: string
                      type: object
                    resources:
                      description: Resources selects which resources to include.
                      properties:
                        all:
                          description: All matches every entry in the source's catalog.
                          type: boolean
                        names:
                          description: Names is an explicit allow-list of entry names.
                          items:
                            type: string
                          type: array
                        prefix:
                          description: Prefix matches any entry whose name starts
                            with this string.
                          type: string
                        regex:
                          description: Regex matches any entry whose name matches
                            this Go regexp.
                          type: string
                      type: object
                    rewrite:
                      description: |-
                        Rewrite renames selected entries with a prefix/suffix to avoid
                        collisions with entries from other sources.
                      properties:
                        prefix:
                          description: Prefix prepended to every selected name.
                          type: string
                        suffix:
                          description: Suffix appended to every selected name.
                          type: string
                      type: object
                    tools:
                      description: |-
                        Tools selects which tools to include. Defaults to none if unset (use
                        {all: true} to include them all).
                      properties:
                        all:
                          description: All matches every entry in the source's catalog.
                          type: boolean
                        names:
                          description: Names is an explicit allow-list of entry names.
                          items:
                            type: string
                          type: array
                        prefix:
                          description: Prefix matches any entry whose name starts
                            with this string.
                          type: string
                        regex:
                          description: Regex matches any entry whose name matches
                            this Go regexp.
                          type: string
                      type: object
                  type: object
                minItems: 1
                type: array
            required:
            - sources
            type: object
          status:
            description: VirtualMCPRouteStatus reflects the observed state of a VirtualMCPRoute.
            properties:
              conditions:
                description: Conditions track Ready / SourceMissing / Conflict semantics.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              endpointURL:
                description: |-
                  EndpointURL is the proxy-fronted MCP URL clients use to reach this
                  route (e.g. http://proxy/api/v1/vroutes/<exposedAs>/mcp).
                type: string
              entryCount:
                description: |-
                  EntryCount is the size of ResolvedEntries; surfaced as its own field
                  so it can be a printer column without paging through the full list.
                format: int32
                type: integer
              lastResolvedTime:
                description: |-
                  LastResolvedTime is when the controller last successfully resolved
                  the catalog.
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the spec generation the controller
                  last acted on.
                format: int64
                type: integer
              phase:
                description: Phase is the high-level rollup of Conditions.
                enum:
                - Pending
                - Resolving
                - Ready
                - Degraded
                - Failed
                type: string
              resolvedEntries:
                description: |-
                  ResolvedEntries is the flattened catalog with origin metadata. Large
                  routes can produce large status payloads — UIs that need only a count
                  should read Status.EntryCount instead.
                items:
                  description: |-
                    ResolvedEntry is one tool / resource / prompt flattened into the route's
                    catalog after selectors and rewrites are applied.
                  properties:
                    kind:
                      description: Kind tells clients which MCP catalog this entry
                        belongs to.
                      enum:
                      - tool
                      - resource
                      - prompt
                      type: string
                    name:
                      description: Name is the externally exposed name (after rewrite).
                      type: string
                    originalName:
                      description: |-
                        OriginalName is the entry's name on its source (pre-rewrite); empty
                        when no rewrite was applied.
                      type: string
                    sourceAdapter:
                      description: SourceAdapter is the Adapter CR name the entry
                        came from, if any.
                      type: string
                    sourceMCPServer:
                      description: SourceMCPServer is the MCPServer CR name the entry
                        came from, if any.
                      type: string
                  required:
                  - kind
                  - name
                  type: object
                type: array
                x-kubernetes-list-type: atomic
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
